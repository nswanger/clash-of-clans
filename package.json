{
  "name": "cwl-ops-assistant",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test && sh scripts/tests/verify-collector.test.sh && sh scripts/tests/check-migrations.test.sh",
    "e2e": "playwright test",
    "migrations:check": "sh scripts/check-migrations.sh",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
